name: flysystem-minio

# ddev constraints:
# - 1.22.5 bumped docker-composer to include docker-composer 2.23.1+, such
#   that inline config maps are usable
# - 1.24.10 facilitated inter-container comms
# - 1.25.0 broke some of the inter-container comms (fixed in 1.25.1)
ddev_version_constraint: '>= v1.25.3'

dependencies:
  - minio

project_files:
  - commands/minio/create-buckets
  - docker-compose.web-flysystem.yaml
  - config.flysystem-minio.yaml
